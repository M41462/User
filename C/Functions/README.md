# Functions

A mathematical function plotter that visualizes sin, cos, tan, exp, ln, linear, square, and absolute value functions.

## How It Works

Console menu lets you pick a function type, then a Raylib window opens and draws the function on a coordinate grid with labeled axes. Uses a 50px scale factor for clean rendering at 60 FPS.

## Build

```bash
make
make run
```
